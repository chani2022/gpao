{% extends 'base-home.html.twig' %}
{% block title %}Identification{% endblock %}

{% block pageTitle %} Identification {% endblock %}

{% block body %}

<div class="col-lg-12 col-md-12">
    <div class="row">
       


        <div class="col-lg-6 col-md-6">
            <div class="card">

                <div class="card-body">
                    
                    {# form search #}
                    {% set id_personnel = app.session.get('id_personnel') %}
                    <form method="get" action="{{ path('app_securite_identification') }}" class="form">
                    <div class="form-group">
                        <input type="text" name="id_personnel" value="{{id_personnel}}" placeholder="Matricule" class="form-control" />
                    </div>
                    <button type="submit" class="btn btn-primary ml-2">
                        Rechercher
                    </button>
                </form>
                {# end form search #}
            </div>
        </div>
    </div>
</div>
</div></div><div class="col-lg-12 col-md-12">
<br/>
<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table">
                    <thead class="thead-light">
                        <tr>
                            <th>Nom</th>
                            <th>Prénom</th>
                            <th>Matricule</th>
                            <th>Photo</th>
                            <th>Equipe</th> 
                            <th>Identification</th> 
                        </tr>
                    </thead>
                    <tbody>
                        {% for tabl in personnel %}
                            <div class="form-group">
                                <tr>
                                    <td>{{tabl.nom }}</td>
                                    <td>{{tabl.prenom}}</td>
                                    <td>{{tabl.id_personnel}}</td>
                                    <td>{{tabl.photo}}</td>
                                    <td>{{tabl.nom_equipe}}</td>
                                    <td>{{tabl.login}}</td>
                                    
                                </tr>
                            </div>
                        {% endfor %}
                    </tbody>
            </table>
        </div>
    </div>
</div>
</div>
</div>





{% endblock %}
{% block javascripts %}
   
{% endblock %}
