{% extends 'base.html.twig' %}

{% block title %}Test{% endblock %}

{% block stylesheets %}
    {{parent()}}
{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>
    {% if app.user %}
        <div class="mb-3">
            Connecté en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}">Quitter</a>
        </div>
    {% endif %}
    {{dump(menus)}}

</div>
{% endblock %}
